Flask==2.3.3
Pillow==10.0.1
Werkzeug==2.3.7

# Note: For GUI (photo_frame_gui.py), tkinter is required but must be installed system-wide:
# sudo apt-get install python3-tk
# tkinter cannot be installed via pip as it's a system package